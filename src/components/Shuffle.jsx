import React from 'react'

const Shuffle = ({cards}) => {
  
  
  // const pastCard = threeCards.filter((card, index) => index % 3 === 0)
  // const presentCard = threeCards.filter((card, index) => index % 3 === 0)
  // const futureCard = threeCards.filter((card, index) => index % 3 === 0)

  return (
    <div>
      {/* <div>
        {pastCard.map((cardpa) => (
          <div key={cardpa.id}>
            <p>Card of the Past</p>
            <img src={cardpa.sakuraCard} alt="" />
            <h3>{cardpa.spanishName}</h3>
            <p>{cardpa.meaning}</p>
          </div>
        ))}
        {presentCard.map((cardpr) => (
          <div key={cardpr.id}>
            <p>Card of the Present</p>
            <img src={cardpr.sakuraCard} alt="" />
            <h3>{cardpr.spanishName}</h3>
            <p>{cardpr.meaning}</p>
          </div>
        ))}
        {futureCard.map((cardfu) => (
          <div key={cardfu.id}>
            <p>Card of the Present</p>
            <img src={cardfu.sakuraCard} alt="" />
            <h3>{cardfu.spanishName}</h3>
            <p>{cardfu.meaning}</p>
          </div>
        ))}      

      </div>       */}
    </div>
  )
}

export default Shuffle